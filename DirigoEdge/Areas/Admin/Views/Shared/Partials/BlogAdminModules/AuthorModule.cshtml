@model DirigoEdge.Areas.Admin.Models.ViewModels.EditBlogViewModel

<h4>Author</h4>

<div class="content">
    <select class=" form-control" id="Author" autocomplete="off">
        @{
            foreach (var user in Model.BlogUsers)
            {
                var selected = user.UserId == Model.ThisBlog.BlogAuthor.UserId ? "selected=\"selected\"" : "";
                <option data-id="@user.UserId" @selected>@user.DisplayName</option>		
            }
        }
    </select>
</div>